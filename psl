#!/bin/bash
#ping camera as specific intervals
INTERVAL=60
IPCAM=$1

if [ -z $1 ]; then
  echo "Usage: psl {ip-adr} {interval}"
  exit 1
fi;

ping -i $INTERVAL -D $IPCAM | while read pong; do if [[ $pong != *"PING"* ]]; then echo "$(date +'%F %T'): $pong" ; fi; done
